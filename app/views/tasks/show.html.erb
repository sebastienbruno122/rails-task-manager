<%= @task.name %>
<%= @task.details %>


<ol>
  <li><%= link_to "back to list", tasks_path %></li>
  <li><%= link_to "Delete #{@task.name}", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %><li>
</ol>
